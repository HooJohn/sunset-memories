# server/.env.example

# Application Port
PORT=3001

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-replace-me
JWT_EXPIRES_IN=1d

# Database Configuration
# For SQLite (current default in the app setup)
# The database file 'db.sqlite' will be created in the 'server/' directory
# by the TypeORM configuration in `server/src/app.module.ts`.
# Ensure this path is writable by the application. This file is in .gitignore.
# No explicit DATABASE_URL is typically needed for this simple SQLite setup in TypeORM.
# DB_TYPE=sqlite # Informational, actual type is set in app.module.ts
# DB_DATABASE_NAME=db.sqlite # Informational

# For PostgreSQL (Example for future use)
# DB_TYPE=postgres
# DATABASE_URL=postgresql://your_db_user:your_db_password@your_db_host:your_db_port/your_db_name
# Example: DATABASE_URL=postgresql://admin:password@localhost:5432/sunset_memories_db

# STT/LLM API Keys (Future - if needed by actual services)
# ALIYUN_ACCESS_KEY_ID=
# ALIYUN_ACCESS_KEY_SECRET=
# TONGYI_QIANWEN_API_KEY=
